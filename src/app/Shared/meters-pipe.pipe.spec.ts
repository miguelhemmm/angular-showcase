import { ConvertorPipe } from './convertor-pipe.pipe';

describe('MetersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
